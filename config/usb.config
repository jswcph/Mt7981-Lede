#=================================================
# usb.config - 带 USB 口的设备专用配置
# 仅在 scripts/part2.sh 的 USB_DEVICES 列表里的设备才会追加。
# 注意：
#   1. 只写 CONFIG_PACKAGE_xxx=y 这类真正的 OpenWrt 选项。
#      CONFIG_USB_SUPPORT 是由目标平台自动选中的隐藏项，写了没有作用，所以不写。
#   2. part2.sh 会在 defconfig 之后逐行核验下面所有 “CONFIG_xxx=y”，
#      任何一行没生效都会让构建直接失败，避免悄悄编出没有 USB 功能的固件。
#   3. 想临时去掉某个包，直接把那一行删掉或整行注释掉即可（核验只看 CONFIG_*=y 行）。
#=================================================

# ---------- USB 控制器 ----------
CONFIG_PACKAGE_kmod-usb3=y

# ---------- USB 存储 / NAS ----------
CONFIG_PACKAGE_kmod-usb-storage=y
CONFIG_PACKAGE_kmod-usb-storage-extras=y
CONFIG_PACKAGE_kmod-usb-storage-uas=y
CONFIG_PACKAGE_block-mount=y
CONFIG_PACKAGE_automount=y

# 文件系统
CONFIG_PACKAGE_kmod-fs-ext4=y
CONFIG_PACKAGE_kmod-fs-vfat=y
CONFIG_PACKAGE_kmod-fs-exfat=y
CONFIG_PACKAGE_kmod-fs-ntfs3=y

# 文件名编码
CONFIG_PACKAGE_kmod-nls-cp437=y
CONFIG_PACKAGE_kmod-nls-iso8859-1=y
CONFIG_PACKAGE_kmod-nls-utf8=y

# ---------- 4G / 5G / CPE 上网卡：内核驱动 ----------
# USB 串口（AT 口、PPP 拨号），option 覆盖绝大多数 3G/4G/5G 模块的串口
CONFIG_PACKAGE_kmod-usb-serial=y
CONFIG_PACKAGE_kmod-usb-serial-wwan=y
CONFIG_PACKAGE_kmod-usb-serial-option=y
CONFIG_PACKAGE_kmod-usb-serial-qualcomm=y
CONFIG_PACKAGE_kmod-usb-serial-sierrawireless=y
CONFIG_PACKAGE_kmod-usb-acm=y
CONFIG_PACKAGE_kmod-usb-wdm=y

# USB 网卡类模块
CONFIG_PACKAGE_kmod-usb-net=y
# QMI：移远 Quectel / 广和通 Fibocom / 美格 Meig / 高通方案 等
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan=y
# MBIM：英特尔方案、部分 Fibocom / 华为 / 联想等
CONFIG_PACKAGE_kmod-usb-net-cdc-mbim=y
# NCM：华为 / 中兴 等
CONFIG_PACKAGE_kmod-usb-net-cdc-ncm=y
CONFIG_PACKAGE_kmod-usb-net-huawei-cdc-ncm=y
# ECM / RNDIS：随身 WiFi、CPE、手机 USB 共享网络（安卓）
CONFIG_PACKAGE_kmod-usb-net-cdc-ether=y
CONFIG_PACKAGE_kmod-usb-net-rndis=y
# Sierra Wireless 的 Direct IP 模式
CONFIG_PACKAGE_kmod-usb-net-sierrawireless=y
# iPhone USB 共享网络
CONFIG_PACKAGE_kmod-usb-net-ipheth=y

# ---------- USB 有线网卡（当 WAN/LAN 扩展口） ----------
CONFIG_PACKAGE_kmod-usb-net-rtl8152=y
CONFIG_PACKAGE_kmod-usb-net-asix=y
CONFIG_PACKAGE_kmod-usb-net-asix-ax88179=y
CONFIG_PACKAGE_kmod-usb-net-aqc111=y

# ---------- 4G / 5G / CPE：拨号与 LuCI 协议 ----------
# QMI 拨号
CONFIG_PACKAGE_uqmi=y
CONFIG_PACKAGE_luci-proto-qmi=y
# MBIM 拨号
CONFIG_PACKAGE_umbim=y
CONFIG_PACKAGE_luci-proto-mbim=y
# NCM 拨号（华为等）
CONFIG_PACKAGE_comgt-ncm=y
CONFIG_PACKAGE_luci-proto-ncm=y
# 传统 3G / PPP 串口拨号 + Sierra Direct IP
CONFIG_PACKAGE_comgt=y
CONFIG_PACKAGE_comgt-directip=y
CONFIG_PACKAGE_luci-proto-3g=y
# 模块插入后自动从“虚拟U盘/CD-ROM 模式”切换成上网卡模式
CONFIG_PACKAGE_usb-modeswitch=y

# ---------- 可选（默认不启用，需要时取消注释） ----------
# ModemManager：新式统一拨号方案，体积较大（glib2 + dbus）。
# 注意：与上面的 uqmi/umbim 同时启用时，同一块网卡只应选一种协议拨号，
# 否则 ModemManager 可能抢占串口影响 uqmi。一般用上面的传统方案就够了。
# CONFIG_PACKAGE_modemmanager=y
# CONFIG_PACKAGE_luci-proto-modemmanager=y
#
# 信号/基站信息面板、短信工具（LuCI 页面）
# CONFIG_PACKAGE_luci-app-3ginfo-lite=y
# CONFIG_PACKAGE_luci-i18n-3ginfo-lite-zh-cn=y
# CONFIG_PACKAGE_luci-app-sms-tool-js=y
